# Konfiguration des firstdorsal-web-Deployments (mpm compose).
hostname: firstdorsal.eu
# Das Image baut und pusht die GitHub-Pipeline (.github/workflows/build.yml).
image: ghcr.io/firstdorsal/firstdorsal-web
tag: latest
